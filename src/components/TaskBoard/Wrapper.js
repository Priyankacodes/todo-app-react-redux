import styled from 'styled-components';

const Wrapper = styled.div`
    position: relative;
    z-index: 1;
    margin-top: 5%;
    margin-left: auto;
    margin-right: auto;
    width: 60%;
`;

export default Wrapper;